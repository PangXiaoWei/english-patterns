// Static-site helper contract for future component extraction.
// The live renderer is renderVocabCard() in app.js to preserve the existing no-build setup.
export function PronunciationCard(item) {
  return item;
}
